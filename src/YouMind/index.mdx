---
title: YouMind
description: https://youmind.ai
category: Application
---

import { YouMind } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';
import DocsPreview from '../components/DocsPreview';

## Icons

<DocsPreview>{<YouMind size={64} />}</DocsPreview>

```tsx
import { YouMind } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => <YouMind size={64} />;
```

## Text

<DocsPreview>{<YouMind.Text size={48} />}</DocsPreview>

```tsx
import { YouMind } from '@lobehub/icons';

export default () => <YouMind.Text size={48} />;
```

## Combine

<DocsPreview>
  {
    <Flexbox gap={16} align={'flex-start'}>
      <YouMind.Combine size={64} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { YouMind } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} align={'flex-start'}>
    <YouMind.Combine size={64} />
  </Flexbox>
);
```

## Avatars

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <YouMind.Avatar size={64} />
      <YouMind.Avatar size={64} shape={'square'} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { YouMind } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} horizontal>
    <YouMind.Avatar size={64} />
    <YouMind.Avatar size={64} shape={'square'} />
  </Flexbox>
);
```

## Colors

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <ColorPreview color={YouMind.colorPrimary} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { YouMind } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';

export default () => (
  <Flexbox gap={16} horizontal>
    <ColorPreview color={YouMind.colorPrimary} />
  </Flexbox>
);
```
