---
title: Cursor
description: https://cursor.com
category: Application
---

import { Cursor } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';
import DocsPreview from '../components/DocsPreview';

## Icons

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <Cursor size={128} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Cursor } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} horizontal>
    <Cursor size={128} />
  </Flexbox>
);
```

## Text

<DocsPreview>{<Cursor.Text size={48} />}</DocsPreview>

```tsx
import { Cursor } from '@lobehub/icons';

export default () => <Cursor.Text size={48} />;
```

## Combine

<DocsPreview>
  {
    <Flexbox gap={16} align={'flex-start'}>
      <Cursor.Combine size={64} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Cursor } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} align={'flex-start'}>
    <Cursor.Combine size={64} />
  </Flexbox>
);
```

## Avatars

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <Cursor.Avatar size={64} type={'color'} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Cursor } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} horizontal>
    <Cursor.Avatar size={64} type={'color'} />
  </Flexbox>
);
```

## Colors

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <ColorPreview color={Cursor.colorPrimary} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Cursor } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';

export default () => (
  <Flexbox gap={16} horizontal>
    <ColorPreview color={Cursor.colorPrimary} />
  </Flexbox>
);
```
