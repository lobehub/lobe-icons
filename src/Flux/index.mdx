---
title: Flux (black forest labs)
description: https://blackforestlabs.ai
category: Model
---

import { Flux } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';
import DocsPreview from '../components/DocsPreview';

## Icons

<DocsPreview>{<Flux size={64} />}</DocsPreview>

```tsx
import { Flux } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => <Flux size={64} />;
```

## Text

<DocsPreview>{<Flux.Text size={48} />}</DocsPreview>

```tsx
import { Flux } from '@lobehub/icons';

export default () => <Flux.Text size={48} />;
```

## Combine

<DocsPreview>
  {
    <Flexbox gap={16} align={'flex-start'}>
      <Flux.Combine size={64} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Flux } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} align={'flex-start'}>
    <Flux.Combine size={64} />
  </Flexbox>
);
```

## Avatars

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <Flux.Avatar size={64} />
      <Flux.Avatar size={64} shape={'square'} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Flux } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} horizontal>
    <Flux.Avatar size={64} />
    <Flux.Avatar size={64} shape={'square'} />
  </Flexbox>
);
```

## Colors

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <ColorPreview color={Flux.colorPrimary} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Flux } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';

export default () => (
  <Flexbox gap={16} horizontal>
    <ColorPreview color={Flux.colorPrimary} />
  </Flexbox>
);
```
