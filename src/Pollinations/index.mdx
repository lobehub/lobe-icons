---
title: Pollinations
description: https://pollinations.ai
category: Application
---

import { Pollinations } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';
import DocsPreview from '../components/DocsPreview';

## Icons

<DocsPreview>{<Pollinations size={64} />}</DocsPreview>

```tsx
import { Pollinations } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => <Pollinations size={64} />;
```

## Text

<DocsPreview>{<Pollinations.Text size={48} />}</DocsPreview>

```tsx
import { Pollinations } from '@lobehub/icons';

export default () => <Pollinations.Text size={48} />;
```

## Combine

<DocsPreview>
  {
    <Flexbox gap={16} align={'flex-start'}>
      <Pollinations.Combine size={64} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Pollinations } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} align={'flex-start'}>
    <Pollinations.Combine size={64} />
  </Flexbox>
);
```

## Avatars

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <Pollinations.Avatar size={64} />
      <Pollinations.Avatar size={64} shape={'square'} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Pollinations } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} horizontal>
    <Pollinations.Avatar size={64} />
    <Pollinations.Avatar size={64} shape={'square'} />
  </Flexbox>
);
```

## Colors

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <ColorPreview color={Pollinations.colorPrimary} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Pollinations } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';

export default () => (
  <Flexbox gap={16} horizontal>
    <ColorPreview color={Pollinations.colorPrimary} />
  </Flexbox>
);
```
