---
title: Sync
description: https://sync.so
category: Application
---

import { Sync } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';
import DocsPreview from '../components/DocsPreview';

## Icons

<DocsPreview>{<Sync size={64} />}</DocsPreview>

```tsx
import { Sync } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => <Sync size={64} />;
```

## Text

<DocsPreview>{<Sync.Text size={48} />}</DocsPreview>

```tsx
import { Sync } from '@lobehub/icons';

export default () => <Sync.Text size={48} />;
```

## Combine

<DocsPreview>
  {
    <Flexbox gap={16} align={'flex-start'}>
      <Sync.Combine size={64} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Sync } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} align={'flex-start'}>
    <Sync.Combine size={64} />
  </Flexbox>
);
```

## Avatars

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <Sync.Avatar size={64} />
      <Sync.Avatar size={64} shape={'square'} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Sync } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} horizontal>
    <Sync.Avatar size={64} />
    <Sync.Avatar size={64} shape={'square'} />
  </Flexbox>
);
```

## Colors

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <ColorPreview color={Sync.colorPrimary} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { Sync } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';

export default () => (
  <Flexbox gap={16} horizontal>
    <ColorPreview color={Sync.colorPrimary} />
  </Flexbox>
);
```
