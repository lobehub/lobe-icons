---
title: opencode
description: https://opencode.ai
category: Application
---

import { OpenCode } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';
import DocsPreview from '../components/DocsPreview';

## Icons

<DocsPreview>{<OpenCode size={64} />}</DocsPreview>

```tsx
import { OpenCode } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => <OpenCode size={64} />;
```

## Text

<DocsPreview>{<OpenCode.Text size={48} />}</DocsPreview>

```tsx
import { OpenCode } from '@lobehub/icons';

export default () => <OpenCode.Text size={48} />;
```

## Combine

<DocsPreview>
  {
    <Flexbox gap={16} align={'flex-start'}>
      <OpenCode.Combine size={64} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { OpenCode } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} align={'flex-start'}>
    <OpenCode.Combine size={64} />
  </Flexbox>
);
```

## Avatars

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <OpenCode.Avatar size={64} />
      <OpenCode.Avatar size={64} shape={'square'} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { OpenCode } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} horizontal>
    <OpenCode.Avatar size={64} />
    <OpenCode.Avatar size={64} shape={'square'} />
  </Flexbox>
);
```

## Colors

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <ColorPreview color={OpenCode.colorPrimary} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { OpenCode } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';

export default () => (
  <Flexbox gap={16} horizontal>
    <ColorPreview color={OpenCode.colorPrimary} />
  </Flexbox>
);
```
