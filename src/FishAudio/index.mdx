---
title: FishAudio (Bert)
description: https://github.com/fishaudio
category: Model
---

import { FishAudio } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';
import DocsPreview from '../components/DocsPreview';

## Icons

<DocsPreview>{<FishAudio size={64} />}</DocsPreview>

```tsx
import { FishAudio } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => <FishAudio size={64} />;
```

## Text

<DocsPreview>{<FishAudio.Text size={48} />}</DocsPreview>

```tsx
import { FishAudio } from '@lobehub/icons';

export default () => <FishAudio.Text size={48} />;
```

## Combine

<DocsPreview>
  {
    <Flexbox gap={16} align={'flex-start'}>
      <FishAudio.Combine size={64} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { FishAudio } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} align={'flex-start'}>
    <FishAudio.Combine size={64} />
  </Flexbox>
);
```

## Avatars

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <FishAudio.Avatar size={64} />
      <FishAudio.Avatar size={64} shape={'square'} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { FishAudio } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

export default () => (
  <Flexbox gap={16} horizontal>
    <FishAudio.Avatar size={64} />
    <FishAudio.Avatar size={64} shape={'square'} />
  </Flexbox>
);
```

## Colors

<DocsPreview>
  {
    <Flexbox gap={16} horizontal>
      <ColorPreview color={FishAudio.colorPrimary} />
    </Flexbox>
  }
</DocsPreview>

```tsx
import { FishAudio } from '@lobehub/icons';
import { Flexbox } from '@lobehub/ui';

import ColorPreview from '../components/ColorPreview';

export default () => (
  <Flexbox gap={16} horizontal>
    <ColorPreview color={FishAudio.colorPrimary} />
  </Flexbox>
);
```
